{
  "name": "cra-redux-express-react-reactrouter",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "npm start --prefix client & nodemon ./bin/www",
    "dev:server": "nodemon ./bin/www",
    "build": "cd client && npm run build",
    "heroku-postbuild": "cd client && npm run build",
    "prettier": "prettier -l --write --single-quote --no-semi --jsx-bracket-same-line \"client/src/**/*.js\""
  },
  "dependencies": {
    "body-parser": "~1.18.2",
    "cookie-parser": "~1.4.3",
    "cors": "^2.8.4",
    "debug": "~2.6.9",
    "express": "~4.15.5",
    "jade": "~1.11.0",
    "morgan": "~1.9.0",
    "serve-favicon": "~2.4.5"
  },
  "devDependencies": {
    "nodemon": "^1.14.7"
  }
}
